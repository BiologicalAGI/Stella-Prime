# Stella Luanti V0.2 — Recovery Checkpoint V0.1

Status: **ORIENTATION / EVIDENCE INDEX / NON-AUTHORITY**

This file is designed so a future session can reconstruct the governed working position without relying on chat memory. Reading it does not authorize any action.

## 1. Where are we?

Stella Luanti V0.2 remains a research/design experiment. The V0.2 Experience Blueprint was pressure-tested and converted into a Build Contract. GitHub research artifacts live on branch `research/luanti-v0.2-build-contract-2026-08-23` in `BiologicalAGI/Stella-Prime`.

## 2. What was last proven?

Research-side proof only: the ownership/authority architecture is coherent enough to proceed to local reverification. Luanti 5.17.0 capabilities materially support the planned embodiment/UI direction, but GitHub/web evidence does not establish the iBUYPOWER's current local state.

## 3. Why stop here?

The next meaningful facts are machine-current facts: executable/version, exact paths, protected hashes, V0.1 receipts, process state, dependency state, and a free isolated V0.2 target. They must be freshly observed before local mutation.

## 4. Safe next

`V2-00R — Fresh Local V0.1 Closure / V0.2 Readiness Reverification` as a read-only machine check.

## 5. Prepared next

A candidate read-only PowerShell card is stored beside this checkpoint. It is prepared code, not evidence that it ran and not standing authority to run it.

## 6. Current authority represented by this checkpoint

None. This checkpoint records prior execution and design state; authorization must come from the live interaction at action time.

## 7. Reverify before mutation

- host identity;
- actual Luanti executable and version;
- visible launcher/shortcut target;
- exact V0.1 playground/world/mod locations;
- protected hashes/receipts where available;
- relevant Luanti/server process state;
- dependency/install state;
- free isolated V0.2 target path;
- local Git state if local Git will participate.

## 8. Holds / limitations

- native formspec vs Flow is not decided;
- Mara model/assets are not selected or license-approved;
- route/path execution is not yet proven;
- companion C0/C1 transport is not selected;
- no model provider or network mode is approved by this file;
- no Sanctuary implementation is active;
- no canonical promotion is implied.

## 9. Architecture relationship

The authoritative Stella world semantics remain separate from Luanti. Luanti is an adapter/experimental body. Mara is an inhabitant. A future companion is a separate actor/port. Presentation, pathfinding, dialogue wording, and model expression do not own world truth.

## 10. If V2-00R passes

Open a separately bounded V2-01 action to create an isolated V0.2 shell. Do not combine that action with dependency installation, canonical promotion, SCOS integration, model/network activation, or V0.1 modification unless each is separately made part of the live authorized scope.

## Recovery classification

`PASS_FOR_ORIENTATION / HOLD_FOR_LOCAL_MUTATION`

Rationale: research continuity is recoverable; local currentness is intentionally not inferred.